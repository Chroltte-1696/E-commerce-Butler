# pyproject.toml

[build-system]
requires = ["setuptools>=68.0", "wheel"]
build-backend = "setuptools.backends._legacy:_Backend"

[project]
name = "ecom-sync-agent"
version = "0.1.0"
description = "AI Agent 驱动的电商多平台商品同步管家"
readme = "README.md"
license = { text = "MIT" }
requires-python = ">=3.10"
authors = [
    { name = "Your Name", email = "your@email.com" },
]
dependencies = [
    "pydantic>=2.5",
    "httpx>=0.27",
    "openai>=1.12",
    "pyyaml>=6.0",
    "python-dotenv>=1.0",
    "rich>=13.7",
    "Pillow>=10.2",
]

[project.optional-dependencies]
dev = [
    "pytest>=8.0",
    "pytest-asyncio>=0.23",
    "ruff>=0.2",
    "mypy>=1.8",
]

[project.scripts]
ecom-sync = "src.main:cli"

[tool.ruff]
line-length = 100
target-version = "py310"

[tool.pytest.ini_options]
testpaths = ["tests"]
asyncio_mode = "auto"
